・ブランチ
変更の記録を分岐して記録していくもの。

git branch
リポジトリ内全てのブランチを一覧表示する。
git branch <branch>
新規ブランチを作成する。
git branch -d <branch>
指定したブランチを削除する、マージされていない変更はGitが削除を拒否するため安全。
git branch -D <branch>
          　　　　　　　”　　　　　　　　　　　　も削除される。特定の開発ラインで行われた全てのコミットを完全に破棄するときに使用。
git branch -m <branch>
現在のブランチ名を変更する。
git branch -a
全てのリモートブランチを一覧表示する。

・プル
ブランチを切って作業をおこなっている最中にdevelop環境が更新される事がある。それを自分へ反映させる事。自分が新規実装している箇所に影響はない。

git pull <remote>
現在のブランチを指定したリモートにおけるコピーをフェッチして、それをローカルのコピーに即時マージする。
git pull - - no - commito <remote>
既定の呼び出しと同様に、リモートコンテンツはフェッチされますが、新しいマージコミットは作成されません。
git pull - - rebase <remote>
リモートブランチをローカルブランチと統合するのではなく、git rebaseを使用する。
git pull - - verbose
プル中に詳細出力が行われ、ダウンロードされてるコンテンツとマージの詳細が表示される。

・コンフリクト
develop環境が更新され、プルときのバージョンの衝突。

・マージ
ブランチを切って行った作業で発生した差分を開発環境へ合体させること。

git merge [ブランチ名]
統合される。

・プルリクエスト
マージ権限がある人にプルをお願いする事。

git request-pull [-p]
リクエストを実行する。
git request-pull <start>
マージする最初のポイントを示す。
git request-pull <url>
比較するリポジトリを示す。ローカル、リモートどちらでも可。
git request-pull [<end>]
比較を終了するエンドポイントを示す。

・フォーク
他者のリモートリポジトリを自分のリモートリポジトリへ複製すること。（クローンはローカルのまま。）

・スタッシュ
ブランチを一時的に避難させたい時に使う。

git stash
シンプルに避難する
git stash save "メッセージ"
メッセージ付きで避難する。
git stash list
避難の一覧を見る。
git stash show
最新の避難の差分の要約を見る。
git stash drop
最新の避難を削除する。
git stash clear
全ての避難を削除する。

・Diff
変更前と後のファイルの比較。ファイル中のどこが変更されたのかも確認できる。

git Diff
git addする前に変更点が見たい時。
git diff --cached
    ” 　した後に変更点が見たい時。
git diff HEAD^
git commitoした後にコミットした箇所を表示したい場合のコマンド。